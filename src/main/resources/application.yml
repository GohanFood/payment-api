spring:
  application:
    name: payment-api

  datasource:
    url: jdbc:postgresql://localhost:5434/paymentdb
    username: paymentapi
    password: paymentapi123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: payment-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: earliest

jwt:
  secret: ${JWT_SECRET}

# ---------------------------------------------------------------------------
# Mercado Pago — Gateway de Pagamento via Cartão
# ---------------------------------------------------------------------------
# Access Token (obrigatório): gere em https://www.mercadopago.com.br/settings/account/credentials
#   TEST (sandbox) para testes | PROD para transações reais
# Public Key: usada no frontend para inicializar MercadoPago.js CardForm
mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:}
  public-key: ${MERCADOPAGO_PUBLIC_KEY:}

server:
  port: 8082

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
